# SharpDPAPI

**Category**: Windows-native tools and transferable sources

**Purpose**: DPAPI credential extraction and analysis on Windows.

**When to use**: Use when Windows execution, credential extraction, DPAPI, ticket handling, ADCS Windows tooling, or BloodHound collection is needed.

## Paths
- `/opt/windows-tools/SharpDPAPI`

## First Commands
- `ls -la /opt/windows-tools/SharpDPAPI`

## Notes
Windows-native/source tool.

## Captured Help
No live help output was captured. Start with the commands above, or run the tool with `-h`/`--help` in the sandbox.
